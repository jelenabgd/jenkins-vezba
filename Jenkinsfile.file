pipeline {
    agent any

    stages {
        stage('Deploy') {
            when {
              expression {
                currentBuild.result == null || currentBuild.result == 'SUCCESS' 
              }
            }
            steps {
                echo "currentBuild.number"
                echo "IME_GRANE: ${env.BRANCH_NAME}"
                // echo "currentBuild.result1: '${currentBuild.result}'" 
            }
        }
    }
}